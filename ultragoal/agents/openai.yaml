interface:
  display_name: "Ultragoal"
  short_description: "Execute durable goals through verified completion"
  default_prompt: "Use $ultragoal to activate or resume this objective and carry it through verified completion."
policy:
  allow_implicit_invocation: false
