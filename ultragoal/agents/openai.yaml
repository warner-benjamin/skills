interface:
  display_name: "Ultragoal"
  short_description: "Run durable, verified implementation goals"
  default_prompt: "Use $ultragoal to pursue this long-running objective through verified, dependency-ordered work."
policy:
  allow_implicit_invocation: false
