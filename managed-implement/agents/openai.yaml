interface:
  display_name: "Managed Implement"
  short_description: "Execute reviewed design plans after implementation approval"
  default_prompt: "Use $managed-implement to implement the reviewed managed workflow design plan after implementation approval."
policy:
  allow_implicit_invocation: false
