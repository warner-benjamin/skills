interface:
  display_name: "Ultraplan"
  short_description: "Research and design execution-ready plans"
  default_prompt: "Use $ultraplan to gather references, research and challenge this objective, and write the smallest independently reviewed Markdown execution plan."
policy:
  allow_implicit_invocation: false
