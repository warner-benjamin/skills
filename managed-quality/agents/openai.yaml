interface:
  display_name: "Managed Quality"
  short_description: "Remove AI residue from integrated code"
  default_prompt: "Use $managed-quality to remove AI shaped residue from the integrated code and reverify behavior."
policy:
  allow_implicit_invocation: false
